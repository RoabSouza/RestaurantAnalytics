// Representa os totais gerais de vendas para o período
export interface VendaTotal {
  total: number;           // Valor total faturado
  quantidade: number;      // Quantidade total de vendas
  ticketMedio: number;     // Valor médio de cada venda
}